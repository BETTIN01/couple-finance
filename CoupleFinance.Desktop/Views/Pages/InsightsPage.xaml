<UserControl x:Class="CoupleFinance.Desktop.Views.Pages.InsightsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:components="clr-namespace:CoupleFinance.Desktop.Views.Components"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <StackPanel>
        <Border Style="{StaticResource GlassCardStyle}">
            <StackPanel>
                <TextBlock Text="Assistente financeiro do casal" Style="{StaticResource CardTitleStyle}" />
                <TextBlock Text="Os diagnÃ³sticos abaixo analisam variaÃ§Ãµes de gasto, uso de cartÃ£o, ritmo das metas e comportamento recente." Style="{StaticResource CardSubtitleStyle}" Margin="0,8,0,0" />
                <Button Content="Atualizar diagnÃ³sticos"
                        Command="{Binding RefreshInsightsCommand}"
                        Style="{StaticResource AccentContainedButtonStyle}"
                        HorizontalAlignment="Left"
                        Margin="0,18,0,0" />
                <components:PeriodOwnershipFilterStrip Margin="0,18,0,0" />
            </StackPanel>
        </Border>

        <WrapPanel Margin="0,0,0,0">
            <Border Style="{StaticResource InvoiceSummaryMetricCardStyle}">
                <StackPanel>
                    <TextBlock Text="Insights gerados" Style="{StaticResource EyebrowTextStyle}" />
                    <TextBlock Text="{Binding InsightOverviewItems.Count}" Style="{StaticResource MetricValueStyle}" Margin="0,10,0,0" />
                    <TextBlock Text="Leituras automÃ¡ticas prontas para orientar a prÃ³xima decisÃ£o financeira." Style="{StaticResource MetricCaptionStyle}" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource BalanceSummaryMetricCardStyle}">
                <StackPanel>
                    <TextBlock Text="DisponÃ­vel para poupar" Style="{StaticResource EyebrowTextStyle}" />
                    <TextBlock Text="{Binding CurrentSnapshot.Projection.AvailableToSave, StringFormat={}{0:C2}}" Style="{StaticResource MetricValueStyle}" Margin="0,10,0,0" />
                    <TextBlock Text="O assistente usa esse espaÃ§o para sugerir metas e reequilÃ­brio." Style="{StaticResource MetricCaptionStyle}" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource ExpenseSummaryMetricCardStyle}" Margin="0,0,0,18">
                <StackPanel>
                    <TextBlock Text="Faturas em aberto" Style="{StaticResource EyebrowTextStyle}" />
                    <TextBlock Text="{Binding TotalOpenInvoices, StringFormat={}{0:C2}}" Style="{StaticResource MetricValueStyle}" Margin="0,10,0,0" />
                    <TextBlock Text="CartÃµes pressionando o orÃ§amento costumam aparecer nos alertas abaixo." Style="{StaticResource MetricCaptionStyle}" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </WrapPanel>

        <ItemsControl ItemsSource="{Binding InsightOverviewItems}" Visibility="{Binding InsightOverviewItems.Count, Converter={StaticResource CountToVisibilityConverter}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource GlassCardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Width="52" Height="52" Background="{Binding AccentBrush}" CornerRadius="16" Opacity="0.18" VerticalAlignment="Top">
                                <materialDesign:PackIcon Kind="{Binding IconKind}" Foreground="{Binding AccentBrush}" Width="22" Height="22" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>

                            <StackPanel Grid.Column="1" Margin="16,0,0,0">
                                <WrapPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold" Margin="0,0,12,0" />
                                    <Border Background="{Binding AccentBrush}" CornerRadius="10" Padding="8,3" Opacity="0.18">
                                        <TextBlock Text="{Binding SeverityLabel}" Foreground="{Binding AccentBrush}" FontSize="11" FontWeight="SemiBold" />
                                    </Border>
                                </WrapPanel>
                                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Style="{StaticResource CardSubtitleStyle}" Margin="0,10,0,0" />
                                <TextBlock Text="{Binding SupportingText}" Style="{StaticResource MetricCaptionStyle}" Margin="0,10,0,0" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Border Style="{StaticResource EmptyStateCardStyle}" Visibility="{Binding InsightOverviewItems.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="LightbulbAutoOutline" Width="38" Height="38" />
                <TextBlock Text="Ainda nÃ£o hÃ¡ diagnÃ³sticos gerados" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,14,0,0" />
                <TextBlock Text="Registre movimentaÃ§Ãµes e depois atualize os insights para receber recomendaÃ§Ãµes automÃ¡ticas." Style="{StaticResource CardSubtitleStyle}" TextAlignment="Center" Margin="0,8,0,0" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
